{
  "name": "my-clipboard-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0" // 使用较新的 Vite 版本
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0" // 使用 Supabase V2 库
  }
}
